{
  "name": "card-game-rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clean, printable rules for the card games people actually play with friends.",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && python3 -m http.server -d dist 4173"
  }
}
